{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) python3 -m py_compile \"$f\" 2>&1 | head -5;; esac; } 2>/dev/null || true",
            "statusMessage": "Checking Python syntax..."
          }
        ]
      }
    ]
  }
}
